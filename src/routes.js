const routes = Object.freeze({
  HOME: '/',
  MOVIES: '/movie',
  MOVIE_DETAILS: '/movie/:movieId',
  MOVIE_CAST: '/movies/:movieId/credits',
  MOVIE_REVIEWS: '/movies/:movieId/reviews',
});

export default routes;
